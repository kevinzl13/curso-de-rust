// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	devToolbar: {
		enabled: false,
	},
	site: 'https://kevinzl13.github.io',
	base: '/curso-de-rust',
});
